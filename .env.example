# PostgreSQL (local: docker-compose up, production: Railway)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/snaplink"

# Redis (local: docker-compose up, production: Railway)
REDIS_URL="redis://localhost:6379"

# Your deployed app URL (no trailing slash)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
